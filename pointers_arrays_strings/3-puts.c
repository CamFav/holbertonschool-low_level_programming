#include "main.h"
/**
 * _puts - function that prints a string to stdout
 * @str: String
 *
 * Return: Alwayss 0
 */
void _puts(char *str)
{
	int i;

	while (i = 0; str[i]; i++)
	;
	_putchar(*(str[i]));
	i++;
}
	_putchar('\n');
	}
