/**
 * _islower - description
 * main - description
 * @c: Letter to check case of
 *
 * Return: Always 1, return 0 if not lower
 */
	int main(void)
{
	int _islower(int c)
{
	if (c >= 'a' && c <= 'z')
	return (1);
		else
	return (0);
}
}
